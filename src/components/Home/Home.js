import React from "react";
import Todos from "../Todos/Todos";

export function Home() {
  return (
    <div>
      Si chicos estos mensajes son mios y son consejos, asi que lean test y no
      se claven en un error
    </div>
  );
}

export default Home;
